---
description: Show Multiagent Brief capability board and setup status
agent: brief-orchestrator
subtask: false
---

Show the project capability board for: $ARGUMENTS

Run `multi-agent-brief capability $ARGUMENTS` if arguments are provided; otherwise run `multi-agent-brief capability`. For web search, list all supported options: Tavily, Exa, Brave, Firecrawl, Serper, runtime_websearch, configure_later. Never ask the user to paste API keys into chat.
